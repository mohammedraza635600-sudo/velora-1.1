import { useParams } from 'react-router-dom'
import { useSettings } from '../lib/cms'
export const About=()=><div className="pt-32 px-5 md:px-10 max-w-3xl min-h-[70vh]"><h1 className="font-serif text-5xl md:text-7xl">About VELORA</h1><p className="mt-8 text-lg leading-relaxed text-olive">VELORA makes menswear for a refined everyday wardrobe: precise patterns, honest fabrics and a silhouette that holds its line. Every piece is designed to be worn often and to last.</p></div>
export function Contact(){const f=useSettings('footer',{}) as any
  return <div className="pt-32 px-5 md:px-10 min-h-[70vh]"><h1 className="font-serif text-5xl md:text-7xl">Contact</h1>{f.email||f.phone?<div className="mt-8 space-y-2 text-lg">{f.email&&<p><a className="underline" href={'mailto:'+f.email}>{f.email}</a></p>}{f.phone&&<p><a className="underline" href={'tel:'+f.phone}>{f.phone}</a></p>}</div>:<p className="mt-8 text-olive">Contact details will appear here once they are added in Admin, Footer.</p>}</div>}
export const POLICIES:Record<string,string>={shipping:'Orders ship within 2 business days. Shipping is free above ₹5,000 and ₹250 below that.',returns:'Unworn items with tags can be returned within 14 days of delivery.',privacy:'We use your details only to process orders and, if you opt in, to send the VELORA list. We never sell your data.',terms:'By placing an order you agree to these terms. Prices are in INR and include applicable taxes.'}
export function Policy(){const {slug=''}=useParams();const p=useSettings('policies',{}) as any;const t=POLICIES[slug]?(p[slug]||POLICIES[slug]):''
  return <div className="pt-32 px-5 md:px-10 max-w-3xl min-h-[70vh]"><h1 className="font-serif text-5xl md:text-7xl capitalize">{t?slug:'Page not found'}</h1>{t.split('\n').map((x:string,k:number)=><p key={k} className="mt-6 text-lg leading-relaxed text-olive">{x}</p>)}</div>}
